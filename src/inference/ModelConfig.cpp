#include "inference/ModelConfig.hpp"

namespace FreeAI {
    namespace Inference {

        // Configuration is mostly static, loaded from ModelConfig::Default()
        // Can be extended to load from config.ini in the future

    }
}